function buttonEvents() {
    const drawButton = document.querySelector("#draw-button");
    drawButton.addEventListener('click', (e) => location.href=`https://roiceee.github.io/PixelPaint`);
}

buttonEvents();